@model List<Contract_Monthly_Claim_System.Models.Claim>
@{
    ViewData["Title"] = "Manager Approval";
}
<div class="container mt-5">
    <h2>Manager - Approvals</h2>
    <table class="table table-bordered mt-3">
        <thead>
            <tr><th>Claim ID</th><th>Lecturer</th><th>Amount</th><th>Verified</th><th>Action</th></tr>
        </thead>
        <tbody>
            @foreach (var c in Model)
            {
                <tr>
                    <td>@c.ClaimId</td>
                    <td>@c.Lecturer?.FullName</td>
                    <td>R @c.TotalAmount</td>
                    <td>@c.SubmittedAt.ToLocalTime().ToString("yyyy-MM-dd")</td>
                    <td>
                        <form asp-action="ManagerDecision" method="post" style="display:inline;">
                            <input type="hidden" name="claimId" value="@c.ClaimId" />
                            <input type="hidden" name="approve" value="true" />
                            <button type="submit" class="btn btn-primary btn-sm">Approve</button>
                        </form>
                        <form asp-action="ManagerDecision" method="post" style="display:inline; margin-left:6px;">
                            <input type="hidden" name="claimId" value="@c.ClaimId" />
                            <input type="hidden" name="approve" value="false" />
                            <button type="submit" class="btn btn-warning btn-sm">Reject</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
